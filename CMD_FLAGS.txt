# Only used by the one-click installer.
# Example:
# --listen --api
# removed: --trust-remote-code
# --tensorcores
--listen --api --extension FPreloader --extension web_search --extension lynx --auto-devices --verbose --character Assistant
